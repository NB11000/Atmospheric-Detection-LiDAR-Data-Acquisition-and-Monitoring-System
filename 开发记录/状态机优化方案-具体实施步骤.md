# 状态机优化方案 — 具体实施步骤

> 对应方案文档：`状态机优化方案-WebAPI本地维护采集卡状态.md`  
> 创建日期：2026-04-18  
> 状态：待实施

---

## 实施概览

共分为 **5 个阶段**，按依赖关系顺序执行。每个阶段包含具体的代码改动说明和验证方法。

```
阶段1: SystemStateService 增加缓存层
  │
阶段2: GrpcServiceImpl 增加命令映射与状态更新入口
  │
阶段3: GrpcClient 子进程侧完善错误码上报
  │
阶段4: 集成联调与验证
  │
阶段5: 可选 — 低频健康检查兜底
```

---

## 阶段 1：SystemStateService 增加缓存层

**目标**：在 `SystemStateService` 中引入本地缓存字段，将快照生成从 gRPC 查询改为读取缓存。

### 步骤 1.1：新增缓存字段

**文件**：`WebAPI/Service/SystemStateService.cs`

在类中新增以下字段：

```csharp
/// <summary>
/// 采集卡状态本地缓存（通过命令响应和主动上报推断更新，避免每次快照都发起 IPC）
/// 使用 volatile 保证多线程可见性，更新时采用整体替换（不可变对象模式）
/// </summary>
private volatile CollectorStateDto _cachedCollectorState = new CollectorStateDto
{
    ProcessConnected = false,
    DeviceOpened = false,
    Acquiring = false,
    Handle = 0,
    LastMessage = "采集子进程未连接",
    Timestamp = DateTime.Now
};
```

### 步骤 1.2：新增 `UpdateCollectorState()` 方法

**文件**：`WebAPI/Service/SystemStateService.cs`

新增公开方法，供 `GrpcServiceImpl` 在收到各类消息时调用：

```csharp
/// <summary>
/// 更新采集卡状态缓存（由 GrpcServiceImpl 在收到消息时调用）
/// 使用不可变对象替换模式，确保并发读写安全
/// </summary>
/// <param name="updater">状态更新函数，接收当前状态副本，返回新状态</param>
public void UpdateCollectorState(Func<CollectorStateDto, CollectorStateDto> updater)
{
    var current = _cachedCollectorState;
    var newState = updater(current);
    newState.Timestamp = DateTime.Now;
    _cachedCollectorState = newState; // volatile 写，保证可见性
    _logger.LogDebug("采集卡状态缓存已更新: ProcessConnected={Connected}, DeviceOpened={Opened}, Acquiring={Acquiring}",
        newState.ProcessConnected, newState.DeviceOpened, newState.Acquiring);
}
```

同时新增一个重置方法：

```csharp
/// <summary>
/// 重置采集卡状态缓存为默认值（子进程断开时调用）
/// </summary>
public void ResetCollectorState()
{
    _cachedCollectorState = new CollectorStateDto
    {
        ProcessConnected = false,
        DeviceOpened = false,
        Acquiring = false,
        Handle = 0,
        LastMessage = "采集子进程未连接",
        Timestamp = DateTime.Now
    };
    _logger.LogInformation("采集卡状态缓存已重置");
}
```

### 步骤 1.3：改造 `GetCollectorStateAsync()`

**文件**：`WebAPI/Service/SystemStateService.cs`

将原方法从 gRPC 查询改为直接读取缓存：

```csharp
// ====== 改造前 ======
public async Task<CollectorStateDto> GetCollectorStateAsync()
{
    // ... 发送 GET_COLLECTOR_STATE gRPC 命令 ...
}

// ====== 改造后 ======
/// <summary>
/// 获取采集卡运行状态（直接读取本地缓存，零 IPC 开销）
/// </summary>
public CollectorStateDto GetCollectorState()
{
    // 读取 volatile 字段，保证获取最新值
    var state = _cachedCollectorState;
    // 同步连接状态（以 _clientStreams 为准）
    if (state.ProcessConnected != IsCollectorConnected())
    {
        state = new CollectorStateDto
        {
            ProcessConnected = IsCollectorConnected(),
            DeviceOpened = state.DeviceOpened,
            Acquiring = state.Acquiring,
            Handle = state.Handle,
            LastMessage = state.LastMessage,
            Timestamp = DateTime.Now
        };
    }
    return state;
}
```

### 步骤 1.4：同步修改 `GetSystemStateAsync()`

**文件**：`WebAPI/Service/SystemStateService.cs`

```csharp
// ====== 改造前 ======
public async Task<SystemStateDto> GetSystemStateAsync()
{
    var collectorState = await GetCollectorStateAsync(); // gRPC 查询
    // ...
}

// ====== 改造后 ======
/// <summary>
/// 获取系统统一状态快照（全部本地读取，零 IPC）
/// </summary>
public SystemStateDto GetSystemState()
{
    var collectorState = GetCollectorState(); // 读缓存
    var laserState = GetLaserState();         // 进程内读取

    return new SystemStateDto
    {
        Server = new ServerStateDto
        {
            IsApiAlive = true,
            Timestamp = DateTime.Now
        },
        Collector = collectorState,
        Laser = laserState,
        UiHints = BuildUiHints(collectorState, laserState),
        Timestamp = DateTime.Now
    };
}
```

> **注意**：原异步方法可保留为异步签名（`Task<SystemStateDto>`）包装同步调用，以兼容已有的 Controller 和 SignalR Hub 调用方。也可以同步化所有调用链路。

### 步骤 1.5：同步修改 `PublishStateChangedAsync()`

**文件**：`WebAPI/Service/SystemStateService.cs`

```csharp
// 改造前
var state = await GetSystemStateAsync(); // 触发 IPC

// 改造后
var state = GetSystemState(); // 纯本地读取
```

### 验证标准

- [ ] `SystemStateService` 编译通过
- [ ] `GetSystemState()` 返回的 `CollectorStateDto` 来自缓存字段
- [ ] `PublishStateChangedAsync()` 不再触发 gRPC 通信
- [ ] Controller 层调用方式适配完成

---

## 阶段 2：GrpcServiceImpl 增加命令映射与状态更新

**目标**：在 gRPC 服务的消息处理循环中，根据 3 种消息类型更新 `SystemStateService` 的采集卡状态缓存。

### 步骤 2.1：新增 `requestId → command` 映射表

**文件**：`WebAPI/Service/GrpcServiceImpl.cs`

在类中新增字段：

```csharp
/// <summary>
/// 存储 requestId 与原始命令名称的映射
/// 用于 command_response 到达时查找对应的命令，以推断状态变更
/// </summary>
private readonly ConcurrentDictionary<string, string> _requestCommandMap = new();
```

### 步骤 2.2：在发送命令时记录映射

**文件**：`WebAPI/Service/GrpcServiceImpl.cs`  
**位置**：`SendCommandToClientAndWaitResponse()` 方法

在注册 `_commandResponseWaiters` 的同时写入映射：

```csharp
// 现有代码（约第 197 行）
_commandResponseWaiters.TryAdd(requestId, tcs);

// 新增：记录 requestId 与命令的映射关系
_requestCommandMap.TryAdd(requestId, command);
```

在 `finally` 块中同步清理：

```csharp
finally
{
    _commandResponseWaiters.TryRemove(requestId, out _);
    _requestCommandMap.TryRemove(requestId, out _); // 同步清理映射
}
```

### 步骤 2.3：在 `command_response` 分支中增加状态推断

**文件**：`WebAPI/Service/GrpcServiceImpl.cs`  
**位置**：`Communicate()` 方法中 `command_response` 分支（当前第 134-144 行）

在 `tcs.SetResult(clientMsg)` 之后，增加状态更新逻辑：

```csharp
else if (clientMsg.MessageType == "command_response")
{
    _logger.LogInformation($"收到[{processId}]指令[{clientMsg.ResponseId}]响应：{clientMsg.Content}");

    // 找到该指令对应的等待器，并设置响应结果
    if (_commandResponseWaiters.TryRemove(clientMsg.ResponseId, out var tcs))
    {
        tcs.SetResult(clientMsg);
    }

    // ===== 新增：根据命令响应推断并更新采集卡状态 =====
    if (processId == SystemStateService.CollectorClientId
        && _requestCommandMap.TryRemove(clientMsg.ResponseId, out var originalCommand))
    {
        UpdateStateFromCommandResponse(originalCommand, clientMsg);
    }
}
```

### 步骤 2.4：实现 `UpdateStateFromCommandResponse()` 方法

**文件**：`WebAPI/Service/GrpcServiceImpl.cs`

```csharp
/// <summary>
/// 根据命令响应推断并更新采集卡状态缓存
/// </summary>
/// <param name="command">原始命令名称</param>
/// <param name="response">子进程返回的响应</param>
private void UpdateStateFromCommandResponse(string command, AdResponse response)
{
    try
    {
        var stateService = _serviceProvider.GetRequiredService<SystemStateService>();

        // 如果命令处理失败，不更新硬件状态，仅记录错误信息
        if (response.ErrorCode == "COMMAND_HANDLE_FAILED")
        {
            stateService.UpdateCollectorState(current => new CollectorStateDto
            {
                ProcessConnected = current.ProcessConnected,
                DeviceOpened = current.DeviceOpened,
                Acquiring = current.Acquiring,
                Handle = current.Handle,
                LastMessage = $"命令[{command}]执行失败：{response.Content}"
            });
            return;
        }

        switch (command)
        {
            case "OPEN_DEVICE":
            case "OPEN_DEVICE_AGAIN":
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = true,
                    DeviceOpened = response.MHandle > 0,
                    Acquiring = current.Acquiring,
                    Handle = response.MHandle,
                    LastMessage = response.Content
                });
                break;

            case "CLOSE_DEVICE":
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = true,
                    DeviceOpened = false,
                    Acquiring = false,
                    Handle = 0,
                    LastMessage = response.Content
                });
                break;

            case "START_AD":
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = true,
                    DeviceOpened = current.DeviceOpened,
                    Acquiring = response.Content == "AD_STARTED",
                    Handle = current.Handle,
                    LastMessage = response.Content == "AD_STARTED" ? "正在采集" : response.Content
                });
                break;

            case "STOP_AD":
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = true,
                    DeviceOpened = current.DeviceOpened,
                    Acquiring = false,
                    Handle = current.Handle,
                    LastMessage = "已停止采集"
                });
                break;

            case "EXIT":
                stateService.ResetCollectorState();
                break;

            // CONFIG_UPDATE、CONFIG_READ、PING、GET_COLLECTOR_STATE 等不影响硬件状态
            default:
                break;
        }
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "根据命令响应更新采集卡状态失败");
    }
}
```

### 步骤 2.5：在 `data_report` 分支中增加状态处理

**文件**：`WebAPI/Service/GrpcServiceImpl.cs`  
**位置**：`Communicate()` 方法中 `data_report` 分支（当前第 118-122 行）

```csharp
if (clientMsg.MessageType == "data_report")
{
    _logger.LogInformation($"收到[{processId}]消息：{clientMsg.Content}");

    // ===== 新增：处理采集子进程的状态类 data_report =====
    if (processId == SystemStateService.CollectorClientId)
    {
        UpdateStateFromDataReport(clientMsg);
    }
}
```

实现方法：

```csharp
/// <summary>
/// 根据 data_report 主动上报消息更新采集卡状态
/// </summary>
private void UpdateStateFromDataReport(AdResponse report)
{
    try
    {
        var stateService = _serviceProvider.GetRequiredService<SystemStateService>();
        var content = report.Content ?? string.Empty;

        // 识别结构化状态消息（预留扩展：以 [STATE] 前缀标识）
        if (content.Contains("[STATE]"))
        {
            // 提取 JSON 部分并解析（未来扩展）
            // var json = content.Substring(content.IndexOf("[STATE]") + 7);
            // var state = JsonSerializer.Deserialize<CollectorStateDto>(json);
            // stateService.UpdateCollectorState(_ => state);
        }
        else
        {
            // 普通消息：仅更新 LastMessage
            stateService.UpdateCollectorState(current => new CollectorStateDto
            {
                ProcessConnected = current.ProcessConnected,
                DeviceOpened = current.DeviceOpened,
                Acquiring = current.Acquiring,
                Handle = current.Handle,
                LastMessage = content
            });
        }
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "处理 data_report 状态更新失败");
    }
}
```

### 步骤 2.6：在 `Error` 分支中增加状态处理

**文件**：`WebAPI/Service/GrpcServiceImpl.cs`  
**位置**：`Communicate()` 方法中 `Error` 分支（当前第 123-131 行）

```csharp
else if (clientMsg.MessageType == "Error")
{
    _logger.LogError($"收到[{processId}]错误消息：{clientMsg.Content}");

    // ===== 新增：根据错误码更新采集卡状态 =====
    if (processId == SystemStateService.CollectorClientId)
    {
        UpdateStateFromError(clientMsg);
    }
}
```

实现方法：

```csharp
/// <summary>
/// 根据错误消息推断并更新采集卡状态
/// </summary>
private void UpdateStateFromError(AdResponse errorMsg)
{
    try
    {
        var stateService = _serviceProvider.GetRequiredService<SystemStateService>();
        var errorCode = errorMsg.ErrorCode ?? "NONE";

        switch (errorCode)
        {
            case "DEVICE_DISCONNECTED":
                // 设备意外断开，全部硬件状态重置
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = true, // 子进程本身还在
                    DeviceOpened = false,
                    Acquiring = false,
                    Handle = 0,
                    LastMessage = $"设备异常断开：{errorMsg.Content}"
                });
                break;

            case "ACQUISITION_FAILED":
                // 采集异常终止
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = true,
                    DeviceOpened = current.DeviceOpened,
                    Acquiring = false,
                    Handle = current.Handle,
                    LastMessage = $"采集异常终止：{errorMsg.Content}"
                });
                break;

            case "DEVICE_OPEN_FAILED":
                // 设备打开失败
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = true,
                    DeviceOpened = false,
                    Acquiring = false,
                    Handle = 0,
                    LastMessage = $"设备打开失败：{errorMsg.Content}"
                });
                break;

            default:
                // 一般性错误或未分类错误，仅更新 LastMessage
                stateService.UpdateCollectorState(current => new CollectorStateDto
                {
                    ProcessConnected = current.ProcessConnected,
                    DeviceOpened = current.DeviceOpened,
                    Acquiring = current.Acquiring,
                    Handle = current.Handle,
                    LastMessage = $"错误：{errorMsg.Content}"
                });
                break;
        }
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "处理错误消息状态更新失败");
    }
}
```

### 步骤 2.7：完善连接/断开时的缓存管理

**文件**：`WebAPI/Service/GrpcServiceImpl.cs`

子进程**连接时**（约第 91-106 行），在发布 `collector_connected` 事件后初始化缓存：

```csharp
if (processId == SystemStateService.CollectorClientId)
{
    try
    {
        var stateService = _serviceProvider.GetRequiredService<SystemStateService>();

        // 初始化缓存：标记进程已连接，硬件状态待后续命令响应填充
        stateService.UpdateCollectorState(_ => new CollectorStateDto
        {
            ProcessConnected = true,
            DeviceOpened = false,
            Acquiring = false,
            Handle = 0,
            LastMessage = "采集子进程已连接，等待设备操作"
        });

        await stateService.PublishStateChangedAsync(
            "collector_connected", "collector", "采集子进程已连接", "数据采集子进程已连接");
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "发布采集子进程连接事件失败");
    }
}
```

子进程**断开时**（`PublishCollectorDisconnectedAsync` 方法内），在发布事件前重置缓存：

```csharp
private async Task PublishCollectorDisconnectedAsync(string processId)
{
    if (processId == SystemStateService.CollectorClientId)
    {
        try
        {
            var stateService = _serviceProvider.GetRequiredService<SystemStateService>();

            // 重置缓存为默认值（兜底保障）
            stateService.ResetCollectorState();

            await stateService.PublishStateChangedAsync(
                "collector_disconnected", "collector", "采集子进程已断开", "数据采集子进程连接已断开");
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "发布采集子进程断开事件失败");
        }
    }
}
```

### 验证标准

- [ ] `GrpcServiceImpl` 编译通过
- [ ] 发送命令后，`_requestCommandMap` 正确记录映射并在响应/超时后清理
- [ ] 每种命令的响应都能触发正确的状态更新
- [ ] 子进程连接时缓存初始化，断开时缓存重置
- [ ] `data_report` 和 `Error` 消息能触发对应的状态更新

---

## 阶段 3：子进程侧完善错误码上报

**目标**：让子进程在硬件异常时通过 `Error` 消息携带有意义的 `ErrorCode`，使 WebAPI 能区分错误类型并正确更新状态。

### 步骤 3.1：修改 `SendErrorMessage()` 支持 `ErrorCode` 参数

**文件**：`ConsoleApp1/Service/GrpcClient.cs`

```csharp
// ====== 改造前 ======
public static void SendErrorMessage(string message)
{
    // ...
    heartbeat.ErrorCode = "NONE"; // 始终为 NONE
    // ...
}

// ====== 改造后 ======
/// <summary>
/// 客户端主动上报错误消息（支持错误码分类）
/// </summary>
/// <param name="message">错误描述</param>
/// <param name="errorCode">错误码（DEVICE_DISCONNECTED / ACQUISITION_FAILED / DEVICE_OPEN_FAILED / GENERAL_ERROR）</param>
public static void SendErrorMessage(string message, string errorCode = "GENERAL_ERROR")
{
    try
    {
        sb3.Clear();
        sb3.Append("客户端[").Append(_processId).Append("]错误消息 -").Append(message);

        var heartbeat = ResponsePool.Get();
        heartbeat.ResponseId = Guid.NewGuid().ToString("N");
        heartbeat.MessageType = "Error";
        heartbeat.Content = sb3.ToString();
        heartbeat.ErrorCode = errorCode;  // 使用传入的错误码
        heartbeat.ProcessId = _processId;

        ResponseQueue.Enqueue(heartbeat);
    }
    catch (Exception ex)
    {
        Program.logger.LogInformation($"错误：{ex.Message}");
    }
}
```

> **向后兼容**：默认参数 `"GENERAL_ERROR"` 确保现有调用点无需修改。

### 步骤 3.2：在关键硬件异常点添加错误上报

**文件**：`ConsoleApp1` 中涉及硬件操作的代码文件（主要是 `AD_Controlcs` 类）

需要在以下场景添加 `SendErrorMessage` 调用：

| 异常场景 | 位置 | ErrorCode |
|---|---|---|
| `ADWork` 线程中 USB 批量读取失败 | `AD_Controlcs` 的采集线程 `catch` 块 | `DEVICE_DISCONNECTED` |
| `ADWork` 线程中数据处理异常导致采集终止 | `AD_Controlcs` 的采集线程 `catch` 块 | `ACQUISITION_FAILED` |
| 设备打开时 `USB1602_OpenDevice` 返回无效句柄 | `Device_Opened()` 方法 | `DEVICE_OPEN_FAILED` |

示例（在采集线程异常处理中）：

```csharp
catch (Exception ex)
{
    ADDataTest_RunFlag = false; // 标记采集停止
    LastStatusMessage = $"采集异常：{ex.Message}";
    
    // 向 WebAPI 上报错误，携带具体错误码
    if (ex is USBDeviceException || ex.Message.Contains("USB"))
    {
        GrpcClient.SendErrorMessage(ex.Message, "DEVICE_DISCONNECTED");
    }
    else
    {
        GrpcClient.SendErrorMessage(ex.Message, "ACQUISITION_FAILED");
    }
}
```

### 验证标准

- [ ] `SendErrorMessage` 新增重载编译通过
- [ ] 现有调用点（无 `errorCode` 参数）正常工作（默认 `GENERAL_ERROR`）
- [ ] 模拟 USB 断开场景，WebAPI 收到 `DEVICE_DISCONNECTED` 错误码并正确更新状态

---

## 阶段 4：集成联调与验证

### 步骤 4.1：基本功能验证

按以下操作序列验证状态推断的正确性：

| 操作序列 | 预期 WebAPI 缓存状态 |
|---|---|
| 1. 启动子进程，等待 gRPC 连接建立 | `ProcessConnected=true, DeviceOpened=false, Acquiring=false` |
| 2. 发送 `OPEN_DEVICE` 命令 | `DeviceOpened=true, Handle>0` |
| 3. 发送 `START_AD` 命令 | `Acquiring=true` |
| 4. 发送 `STOP_AD` 命令 | `Acquiring=false` |
| 5. 发送 `CLOSE_DEVICE` 命令 | `DeviceOpened=false, Handle=0` |
| 6. 终止子进程 | `ProcessConnected=false, DeviceOpened=false, Acquiring=false` |

### 步骤 4.2：异常场景验证

| 异常场景 | 预期行为 |
|---|---|
| 子进程崩溃（kill 进程） | gRPC 流中断 → `finally` 触发 → 缓存重置 |
| 命令执行失败（`COMMAND_HANDLE_FAILED`） | 状态不变，`LastMessage` 更新为错误信息 |
| `OPEN_DEVICE` 时设备未插入 | `MHandle=0` → `DeviceOpened=false` |
| 采集中 USB 断开（子进程上报 `Error`） | `Acquiring=false, DeviceOpened=false` |

### 步骤 4.3：性能验证

对比改造前后 `GET /api/system/state` 接口的响应时间：

```bash
# 使用工具进行简单压测（如 curl 循环或 wrk）
# 改造前：预期包含 gRPC 往返延迟（毫秒级）
# 改造后：预期纯内存操作（微秒级）
```

### 步骤 4.4：一致性验证

临时启用 `GET_COLLECTOR_STATE` 命令作为校验工具：

```csharp
// 调试代码（验证完成后移除或保留为诊断 API）
var cached = stateService.GetCollectorState();
var actual = await _grpcService.SendCommandToClientAndWaitResponse(CollectorClientId, "GET_COLLECTOR_STATE");
var actualState = JsonSerializer.Deserialize<CollectorStateDto>(actual.Content);

// 对比 cached 与 actualState 的各字段
_logger.LogInformation("缓存 vs 实际: DeviceOpened={Cached}/{Actual}, Acquiring={CachedAcq}/{ActualAcq}",
    cached.DeviceOpened, actualState.DeviceOpened, cached.Acquiring, actualState.Acquiring);
```

### 验证标准

- [ ] 所有操作序列的状态推断结果与预期一致
- [ ] 所有异常场景处理正确
- [ ] 接口响应时间显著降低
- [ ] 缓存状态与子进程实际状态在各场景下保持一致

---

## 阶段 5（可选）：低频健康检查兜底

**目标**：以极低频率定时校验缓存与子进程实际状态的一致性，防止极端情况下的状态漂移。

### 步骤 5.1：添加定时校验任务

**文件**：`WebAPI/Service/SystemStateService.cs`

可使用 `IHostedService` 或 `Timer` 实现：

```csharp
/// <summary>
/// 可选：低频健康检查，定时校验缓存与子进程实际状态的一致性
/// 频率：每 60 秒一次（不在快照热路径中）
/// </summary>
public async Task HealthCheckAsync()
{
    if (!IsCollectorConnected()) return;

    try
    {
        var response = await _grpcService.SendCommandToClientAndWaitResponse(
            CollectorClientId, "GET_COLLECTOR_STATE");
        var actualState = JsonSerializer.Deserialize<CollectorStateDto>(response.Content, JsonOptions);

        if (actualState == null) return;

        var cached = _cachedCollectorState;

        // 检测不一致
        if (cached.DeviceOpened != actualState.DeviceOpened
            || cached.Acquiring != actualState.Acquiring
            || cached.Handle != actualState.Handle)
        {
            _logger.LogWarning(
                "状态缓存与子进程不一致！缓存: DeviceOpened={CachedOpened}, Acquiring={CachedAcq} | " +
                "实际: DeviceOpened={ActualOpened}, Acquiring={ActualAcq}，已修正为实际值",
                cached.DeviceOpened, cached.Acquiring,
                actualState.DeviceOpened, actualState.Acquiring);

            // 以子进程实际状态为准修正缓存
            actualState.ProcessConnected = true;
            _cachedCollectorState = actualState;
        }
    }
    catch (Exception ex)
    {
        _logger.LogWarning(ex, "健康检查失败");
    }
}
```

### 实施建议

- 此阶段为可选项，建议在阶段 4 验证通过后再决定是否启用
- 如果阶段 4 的一致性验证结果良好，可以将健康检查频率设为更低（如 5 分钟）或完全不启用
- 健康检查本身会产生 IPC，频率不宜过高

---

## 完整改动文件清单

| 序号 | 文件路径 | 改动阶段 | 改动类型 |
|---|---|---|---|
| 1 | `WebAPI/Service/SystemStateService.cs` | 阶段 1 | 新增缓存字段、`UpdateCollectorState()`、`ResetCollectorState()`；改造 `GetCollectorStateAsync()` 和 `GetSystemStateAsync()` |
| 2 | `WebAPI/Service/GrpcServiceImpl.cs` | 阶段 2 | 新增 `_requestCommandMap`；`Communicate()` 三个分支增加状态更新；新增 `UpdateStateFromCommandResponse()`、`UpdateStateFromDataReport()`、`UpdateStateFromError()` 方法；完善连接/断开缓存管理 |
| 3 | `ConsoleApp1/Service/GrpcClient.cs` | 阶段 3 | `SendErrorMessage()` 新增 `errorCode` 参数 |
| 4 | `ConsoleApp1/AD_Controlcs.cs`（或相关硬件操作文件） | 阶段 3 | 关键异常 `catch` 块增加 `SendErrorMessage` 调用 |
| 5 | `WebAPI/Protos/Grpc.proto` | — | **无需修改** |
| 6 | `WebAPI/Controllers/SystemStateController.cs` | 阶段 1 | 适配方法签名变更（`async` → 同步，或保留异步包装） |

---

## 回滚方案

如果上线后发现状态推断存在系统性偏差，可快速回滚：

1. 将 `GetCollectorState()` 恢复为原来的 `GetCollectorStateAsync()`（使用 `GET_COLLECTOR_STATE` gRPC 查询）
2. `GrpcServiceImpl` 中新增的状态更新调用可保留不删除（作为 `LastMessage` 的额外更新来源）
3. `SendErrorMessage` 的 `errorCode` 参数改动向后兼容，无需回滚
