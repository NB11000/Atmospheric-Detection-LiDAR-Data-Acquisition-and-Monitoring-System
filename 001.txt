观察，我的webapi项目目前注册了一个应用程序关闭后事件，
用于数据采集子进程随主进程退出而退出的优雅关闭逻辑，但是我发现 这个逻辑只有当我在控制台，按下ctrl c时才会触发，
我直接关闭控制台窗口，这个逻辑不会触发。
现在我需要一个健壮的数据采集子进程随主进城退出而退出的严密逻辑，不仅是主进程正常退出，也可以是主进程非正常退出，
另外，我要求。数据采集子进程不管如何退出都必须要严密遵守以下流程：当设备正在采集时，停止采集，关闭设备，然后退出程序。以上流程在设备正在采集时执行。
我需要，数据采集子进程能够根据当前的设备状态自动判断对以何种方式退出，以避免任何退出时造成的资源泄露或设备控制混乱
注意，不要使用windows上面的专用api要跨平台。


当前 我的主进程启动子进程的逻辑，只是一个简单的调用方法，没有自动重启等其他的高级功能。
我希望你能包装一个类，来封装管理子进程的逻辑。 注意未来可能不止一个子进程可能有多个所以我希望。 多个子进程动轮复用这一个逻辑

现在仔细观察，我的w e b a p i项目和c o n s o l e a p p1项目。

创建一个文件夹 将你的这次优化写成markdown文件放在这个文件夹中

这个文件夹的名称你得修改一下。 我希望这个文件夹不仅有这次跨平台进程监控的优化还有其他的优化


继续修改，此控制器的作用是为了转发前端的命令，然后控制器通过g r p c服务向数据采集子进程发送命令，
客户端进程ID，直接硬编码成数据采集子进程，而不是我们直接在u r l中输入参数。同时，SendCommandRequest这个类应该取消，它里面就一个属性没必要封装成一个类。
并且这个控制器的名称应该修改一下，因为这个控制器只针对于转发命令给数据采集子进程，现在给我一个计划

优化了webapi服务器进程退出时，数据采集子进程无法及时感知问题。
采用进程句柄继承 + WaitForExitAsync的事件驱动监控模式，使得webapi进程在所有退出场景，
数据采集子进程都能及时感知，并跟随退出

创建一个文件夹，把你的这次开发记录写成markdown文件放在这个文件夹中。我希望 此次开发记录有日期和内容。这个文件夹，还会包含其他的开发记录，但目前只有你这一个记录