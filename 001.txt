观察，我的webapi项目目前注册了一个应用程序关闭后事件，
用于数据采集子进程随主进程退出而退出的优雅关闭逻辑，但是我发现 这个逻辑只有当我在控制台，按下ctrl c时才会触发，
我直接关闭控制台窗口，这个逻辑不会触发。
现在我需要一个健壮的数据采集子进程随主进城退出而退出的严密逻辑，不仅是主进程正常退出，也可以是主进程非正常退出，
另外，我要求。数据采集子进程不管如何退出都必须要严密遵守以下流程：当设备正在采集时，停止采集，关闭设备，然后退出程序。以上流程在设备正在采集时执行。
我需要，数据采集子进程能够根据当前的设备状态自动判断对以何种方式退出，以避免任何退出时造成的资源泄露或设备控制混乱
注意，不要使用windows上面的专用api要跨平台。


当前 我的主进程启动子进程的逻辑，只是一个简单的调用方法，没有自动重启等其他的高级功能。
我希望你能包装一个类，来封装管理子进程的逻辑。 注意未来可能不止一个子进程可能有多个所以我希望。 多个子进程动轮复用这一个逻辑


